#!/bin/bash
#
# Kills coreaudiod on macOS.
#
# This fixes the "no sound" issue that happens occasionally.
#
# sudo kill `ps -ax | grep 'coreaudiod' | grep 'sbin' |awk '{print $1}'`
sudo pkill -9 -f coreaudio
