# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: 3

tasks:
  install:
    desc: Install Homebrew and tools defined in Brewfile
    platforms: [darwin]
    cmds:
      - task: install:homebrew
      - task: install:tools

  install:homebrew:
    internal: true
    platforms: [darwin]
    status:
      - which brew
    cmds:
      # Install instructions from https://brew.sh/
      - /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

  install:tools:
    internal: true
    platforms: [darwin]
    deps:
      - install:homebrew
    dir: "/{{.ROOT_DIR}}/configs/homebrew"
    cmds:
      # This is needed here to make sure brew is in the path before the bundle bellow works
      - eval "$(/opt/homebrew/bin/brew shellenv zsh)"
      # https://docs.brew.sh/Manpage#bundle-subcommand
      - brew bundle --file=Brewfile
